[package]
name = "code-thanks-festival-2017-open"
version = "0.1.0"
edition = "2018"

# dependencies added to new project
[dependencies]
proconio = "*"
memoise = "*"
num = "0.4.0"

[profile.release]
lto = true
panic = 'abort'
