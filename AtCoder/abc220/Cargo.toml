[package]
name = "abc220"
version = "0.1.0"
edition = "2018"

# dependencies added to new project
[dependencies]
proconio = "*"
memoise = "0.3.2"
superslice = "1.0.0"
primes = "0.3.0"

[profile.release]
lto = true
panic = 'abort'
