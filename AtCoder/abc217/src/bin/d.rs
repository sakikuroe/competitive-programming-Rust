use proconio::input;

fn main() {
        // Input
        input! {
                l: usize, q: usize,
                cx_list: [(usize, usize); q],
        }
    
        // Initialize
        
        // Solve
        
        // Output
}
