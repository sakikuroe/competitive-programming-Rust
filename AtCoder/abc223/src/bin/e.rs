use proconio::input;

fn main() {
        // Input
        input! {
                x: usize,
                y: usize,
                a: usize,
                b: usize,
                c: usize,
        }

        // Initialize

        // Solve

        // Output
}
