use proconio::input;

fn main() {
        // Input
        input! {
                n: usize,
                h_list: [usize; n],
                a_list: [usize; n],
        }

        // Initialize

        // Solve

        // Output
}
